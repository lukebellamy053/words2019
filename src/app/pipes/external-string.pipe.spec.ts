import { ExternalStringPipe } from './external-string.pipe';

describe('ExternalStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ExternalStringPipe();
    expect(pipe).toBeTruthy();
  });
});
